/* recommendations.y - Вертикальная позиция блока */

// Берем за основу позицию из curb.y, чтобы кнопки были СТРОГО под первым бордюром
extern int curb_1_y; 

// Центрируем кнопки между двумя бордюрами
int rec_base_y = 240; // Фиксированная высота для ряда рекомендаций

int get_recommendation_y() {
    return rec_base_y;
}
